export const FilterBrother = ({
  value,
  onChange,
  onReset,
}: {
  value: string;
  onChange: (newValue: string) => void;
  onReset: () => void;
}) => {
  return (
    <div className="d">filtro de hermanos</div>
  )
}